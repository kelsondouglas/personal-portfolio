import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Kelson Douglas | Web Developer",
  description:
    "Sou um desenvolvedor web, me desenvolvendo cada vez mais na área com tecnologias como NextJS, React, Angular e serviços de cloud como AWS.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-br">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
